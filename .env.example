# Copy to .env and fill in. docker-compose.yml reads this file to
# inject OPENWEATHER_API_KEY into the backend container. Get a free
# key at https://openweathermap.org/api.
OPENWEATHER_API_KEY=your_openweather_api_key_here
